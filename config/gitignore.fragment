# --- focus-kit ---
# Claude Code: what belongs to the session, not to the project
.claude/settings.local.json

# graphify: the graph and its extraction cache (graphify-out/) are
# versioned, so a clone does not pay the extraction again; machine-specific
# paths and the local cost ledger are not
graphify-out/cost.json
graphify-out/.graphify_python
graphify-out/.graphify_root
